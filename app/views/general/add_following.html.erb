<% @users.each do |user| %>

    <section class="user_info">
      <h1>
				<%= image_tag user.picture.url if user.picture? %>
        <%= gravatar_for user if not user.picture? %>
        <%= user.username %></h2>
				<%= render 'users/follow_form'%>
      </h1>
			
    </section>

<% end %>